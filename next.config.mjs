/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js dev-tools "N" indicator — pin it to the bottom-right (default is bottom-left).
  devIndicators: {
    position: "bottom-right",
  },
};

export default nextConfig;
